<html>
  <head>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>

    <script>
      {{component "js-countdown"}}
    </script>

    <style>
      {{component "css-global-reset"}}
      {{component "css-global-styles"}}
      {{component "css-panel-page"}}
    </style>
  </head>
  <body id="about-page">
    <div class="panel-container">
      <div class="panel-content">
        <div style="margin-bottom:10px;">
          <h2>Step 2: Push the register button within two minutes</h2>
        </div>
        <div style="width:600px; font-size:11px; line-height:16px; margin-bottom:40px;">
          To verify physical access to the device, push the register button inside the
          cabinet within two minutes. Refer to our guide, <a href="#">how to find the register button</a>, for more details.
        </div>
        <div id="countdown-timer" data-device-id="{{pageData.device.id}}" data-register-start="{{pageData.device.tags.registerStartTime.[0]}}" data-current-time="{{pageData.currentTime}}" style="font-size:89px; text-align:center;">
          ---
        </div>
        <div id="countdown-timer-label" style="font-size:17px; text-align:center;">
          Time Remaining
        </div>
        <div id="countdown-failure" style="font-size:14px; width:410px; line-height:20px; display:none; padding:20px; box-shadow: 0px 3px 6px 0px rgba(0,0,0,0.10);">
          Failed to detect a register button push within the two minute time limit.
          Please confirm the device is online and attempt the <a href="/register">registration</a> process again.
        </div>
      </div>
    </div>
  </body>
</html>